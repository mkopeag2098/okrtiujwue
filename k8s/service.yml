apiVersion: v1
kind: Service
metadata:
  name: wuoug
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "wuoug"
    port: 8080
  selector:
    app: wuoug
