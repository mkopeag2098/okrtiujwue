apiVersion: v1
kind: Service
metadata:
  name: wangpa
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "wangpa"
    port: 8080
  selector:
    app: wangpa
