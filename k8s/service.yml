apiVersion: v1
kind: Service
metadata:
  name: haoset
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "haoset"
    port: 8080
  selector:
    app: haoset
